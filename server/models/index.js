const { Collections, Roles, RolesEnums } = require("./utils");
const Post = require("./Post");
const User = require("./User");
const Comment = require("./Comment");

module.exports = { Post, User, Comment, Collections, Roles, RolesEnums };
